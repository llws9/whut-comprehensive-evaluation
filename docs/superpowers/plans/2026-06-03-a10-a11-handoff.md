# A-10 / A-11 接力文档（clear 后续跑）

> 目标：你执行 `/clear` 后，下一轮能无缝继续 A-10/A-11，不需要重新梳理上下文。

## 1) 当前状态快照

- 分支：`feat/a10-a5-a8-pr`
- 当前工作目录（worktree）：`.worktrees/a10-role-create`
- 你已确认范围：**仅后端接口+测试**（不含前端改造）
- 你已确认 roleScope 口径：**沿用现有值（当前仅 ORG_SUBTREE）**

### 已完成

1. A-10/A-11 设计说明（spec）
   - 文件：`docs/superpowers/specs/2026-06-03-a10-a11-role-admin-design.md`
   - commit：`23e7252` `docs(spec): add A-10 A-11 role admin design`

2. A-10/A-11 实施计划（plan）
   - 文件：`docs/superpowers/plans/2026-06-03-a10-a11-role-admin-implementation.md`
   - 说明：包含按 TDD 的 task 拆解、命令、提交建议

### 未完成（待执行）

- A-10：`POST /api/admin/roles` 创建角色模板
- A-11：`PATCH /api/admin/roles/{roleId}` 快照并发更新
- 对应测试（注解契约/WebMvc/应用层/仓储）

---

## 2) 下一轮推荐执行方式

推荐使用：**Subagent-Driven Development**（逐 task 执行 + 评审）

如果你下一轮想直接执行，可给 Claude 的第一句话：

```text
请按 docs/superpowers/plans/2026-06-03-a10-a11-role-admin-implementation.md 执行 A-10/A-11，使用 TDD，完成后汇总测试结果与提交记录。
```

---

## 3) 已确认的关键约束（避免跑偏）

1. 仅做后端接口 + 测试，不做前端联调。
2. roleScope 本轮仅允许 `ORG_SUBTREE`。
3. A-11 并发校验采用“字段快照比较”，不引入 version 字段。
4. 冲突码：快照冲突 / roleCode 冲突都返回 `409 BIZ-4090`。

---

## 4) 预期新增/修改文件（执行前核对）

### Interfaces
- `whut-eval-interfaces/src/main/java/edu/whut/eval/interfaces/iam/RoleAdminController.java`（新增 POST/PATCH）
- `whut-eval-interfaces/src/main/java/edu/whut/eval/interfaces/iam/request/CreateRoleRequest.java`（新增）
- `whut-eval-interfaces/src/main/java/edu/whut/eval/interfaces/iam/request/UpdateRoleRequest.java`（新增）
- `whut-eval-interfaces/src/main/java/edu/whut/eval/interfaces/iam/response/RoleCreatedResponse.java`（新增）

### Application
- `whut-eval-application/src/main/java/edu/whut/eval/application/iam/service/RoleAdminApplicationService.java`（新增）
- `whut-eval-application/src/main/java/edu/whut/eval/application/iam/command/CreateRoleCommand.java`（新增）
- `whut-eval-application/src/main/java/edu/whut/eval/application/iam/command/UpdateRoleCommand.java`（新增）
- `whut-eval-application/src/main/java/edu/whut/eval/application/iam/query/RoleCreatedView.java`（新增）

### Domain / Infra
- `whut-eval-domain/src/main/java/edu/whut/eval/domain/iam/model/IamRoleDetail.java`（新增）
- `whut-eval-domain/src/main/java/edu/whut/eval/domain/iam/repository/RoleAdminCommandRepository.java`（新增）
- `whut-eval-infra/src/main/java/edu/whut/eval/infra/persistence/repository/MybatisPlusRoleAdminCommandRepository.java`（新增）

### Tests
- `whut-eval-app/src/test/java/edu/whut/eval/app/iam/RoleAdminControllerSecurityAnnotationTest.java`（新增）
- `whut-eval-app/src/test/java/edu/whut/eval/app/iam/RoleAdminQueryControllerWebMvcTest.java`（修改）
- `whut-eval-app/src/test/java/edu/whut/eval/app/iam/RoleAdminApplicationServiceTest.java`（新增）
- `whut-eval-app/src/test/java/edu/whut/eval/app/infra/MybatisPlusRoleAdminCommandRepositoryTest.java`（新增）

---

## 5) 关键测试命令（执行时复用）

### 单项回归
```bash
mvn -pl whut-eval-app -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=RoleAdminControllerSecurityAnnotationTest test

mvn -pl whut-eval-app -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=RoleAdminQueryControllerWebMvcTest test

mvn -pl whut-eval-app -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=RoleAdminApplicationServiceTest test

mvn -pl whut-eval-app -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=MybatisPlusRoleAdminCommandRepositoryTest test
```

### A-10/A-11 定向总回归
```bash
mvn -pl whut-eval-app -am -Dsurefire.failIfNoSpecifiedTests=false \
  -Dtest=RoleAdminControllerSecurityAnnotationTest,RoleAdminQueryControllerWebMvcTest,RoleAdminApplicationServiceTest,MybatisPlusRoleAdminCommandRepositoryTest test
```

---

## 6) 建议提交切分（执行完成后）

1. `feat(iam): add role create api with roleScope validation`
2. `feat(iam): add role update api with snapshot conflict check`
3. `feat(iam): persist role create and update commands`
4. `docs(plan): add A-10 A-11 handoff summary`（可选，若文档有增量）

---

## 7) 风险提示

- `roleScope` 当前只允许 `ORG_SUBTREE`，若联调方传其他值会返回 400（这是本轮既定策略）。
- A-11 快照字段比较建议统一 trim 后比较，避免“空格差异”误判冲突。

---

## 8) 清理前最后检查

执行 `/clear` 前建议确认：

```bash
git status --short
git log --oneline -n 5
```

确保你希望保留的文件都已提交/推送。

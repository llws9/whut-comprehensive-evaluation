package edu.whut.eval.application.iam.query;

public record UserAdminView(Long userId,
                            String userNo,
                            String userName,
                            String status) {
}
